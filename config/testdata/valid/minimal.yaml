# yaml-language-server: $schema=../../schema.json

server:
  host: 0.0.0.0
  port: 8080

database:
  driver: mssql
  url: ${DATABASE_URL}

api:
  prefix: /api
  resources:
    - name: patients
      base_path: /patients
      routes:
        - method: GET
          query:
            sql: |
              SELECT * FROM patients
          returns: many