---
title: trim
description: Remove characters defined in cutset from both ends of a string.
---

# trim

Removes characters defined in `cutset` from both ends of a string.

## Signature

```hcl
trim(str: string, cutset: string) -> string
```
