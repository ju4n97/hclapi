# Manifest

Reference for the top-level HCL blocks: how files are merged into a single
manifest, and the syntax for `server`, `connection`, `schema`, and
`endpoint`.

For the pipeline steps declared inside an `endpoint`, see
[Pipeline steps](../steps/README.md).
