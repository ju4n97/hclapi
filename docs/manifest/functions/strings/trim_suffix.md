---
title: trim_suffix
description: Remove a suffix from the end of a string if present.
---

# trim_suffix

Removes a suffix from the end of a string if present.

## Signature

```hcl
trim_suffix(str: string, suffix: string) -> string
```
