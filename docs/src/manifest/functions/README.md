# Functions

hclapi provides built-in functions for request-time HCL expression evaluation. Functions can be called in `args`, `condition`, `headers`, `key`, and `body` attributes across all pipeline steps.
